# U1 — Voice transcription (WhatsApp + Telegram)

## Source (v1)
- Primary: `docs/v1-fork-reference/src/transcription.ts` (140 LOC; exports `transcribeAudioBuffer`, `transcribeAudioMessage`, `isVoiceMessage`)
- Helper: `scripts/whisper_transcribe.py` (production tree; faster-whisper, CPU+int8, model cache at `data/whisper-models/`, env `WHISPER_MODEL` default `base`)
- Call sites: `docs/v1-fork-reference/src/channels/whatsapp.ts:39,376-383` + `src/channels/telegram.ts:10,341-375`
- Origin commits: `c054cbc` (upstream skill — OpenAI), `0ccecbd` (upstream skill — local whisper.cpp), `468caba` (fork: faster-whisper + Telegram), `77e476d` (fork: voice-transcription bugfix). No relevant hotfix.

## Behavioral spec (one paragraph)
When an inbound voice message arrives on any registered channel (WhatsApp `audioMessage.ptt=true` via Baileys, or Telegram `message:voice` via grammY), the channel adapter downloads the audio buffer (`downloadMediaMessage` / Bot API `getFile`+fetch) and routes it through a **channel-agnostic** `transcribeAudioBuffer(buffer): Promise<string|null>` helper. The helper tries local faster-whisper first (subprocess to `scripts/whisper_transcribe.py`, 60s timeout, `WHISPER_PYTHON` interpreter override allowed); on null/error it falls back to the OpenAI Whisper API (`whisper-1`, `response_format: 'text'`, key from `OPENAI_API_KEY`); on full failure it returns the fixed string `[Voice Message - transcription unavailable]`. The transcript replaces the otherwise-empty message content before storage/routing so the agent receives voice notes as text. Reason: zero-cost path when local works, reliable fallback when it doesn't, and unified surface across channels — voice notes are first-class user input.

## v2 hook point(s)
- **WhatsApp inbound handler:** `src/channels/whatsapp.ts` in the `nanoclaw-whatsapp` fork. **Cherry-pick the feature commit `c054cbc`** (skill/voice-transcription's actual content) onto `whatsapp-fork/main` — *NOT* a branch merge. The skill branch is 19 commits ahead with only 1-2 feature commits; the rest is "merge main into skill" noise that would pull in 8K LOC of unrelated drift. Then layer the Linux-backend swap and `transcribeAudioBuffer` extraction on top.
- **Telegram inbound handler:** `src/channels/telegram.ts` in the `nanoclaw-telegram` fork — `bot.on('message:voice', ...)` **already registered** in `telegram-fork/main` (~line 311) as a bare placeholder (`storeMedia(ctx, '[Voice message]', {fileId, filename})`; no download, no transcription). Scope: **replace the handler body** to download the buffer via Bot API `getFile` + fetch, call `transcribeAudioBuffer(buffer)`, replace the `[Voice message]` placeholder with the transcript. No new registration needed. Upstream has no Telegram voice support; the fork's existing bare handler is what U1 enhances.
- **Shared helper placement:** `src/transcription.ts` (channel-agnostic file in v2 core, exporting `transcribeAudioBuffer(buffer)`). Upstream's helper is WhatsApp-bound (`transcribeAudioMessage(msg, sock)`); extract the buffer-only entrypoint for cross-channel reuse.
- **Backend script:** `scripts/whisper_transcribe.py` preserved verbatim from v1.

## v2-native equivalent that might suffice?
**PARTIAL-OVERLAP.** Upstream's `whatsapp/skill/voice-transcription` (specifically commit `c054cbc`) provides the OpenAI Whisper path with feature parity (same fallback string, same `whisper-1` model, same `response_format: 'text'`, same `isVoiceMessage` predicate). Upstream's `.claude/skills/use-local-whisper` (commit `0ccecbd`) provides a local-whisper option but via the `whisper-cli` binary (Homebrew, macOS-oriented). Gaps vs. v1: (a) Linux backend for local — keep v1's faster-whisper Python (do NOT cherry-pick `0ccecbd` — it assumes Homebrew; replay the Linux subprocess pattern from v1's `transcription.ts` instead); (b) channel-agnostic `transcribeAudioBuffer` helper — extract from `c054cbc`'s WhatsApp-bound version; (c) Telegram voice support — re-body the existing placeholder handler in `telegram-fork/main`. Net work: 1 cherry-pick on WA fork + ~50 LOC fork delta (helper extraction + Linux backend in transcription.ts + TG handler body replacement).

## Decisions / open questions
1. **Local-whisper backend on Linux: KEEP v1's faster-whisper Python (decided 2026-05-19).** Already working on production Linux server; switching to upstream's `whisper-cli` requires building whisper.cpp from source (Homebrew path is macOS-only); faster-whisper + CTranslate2 + int8 is competitive with whisper.cpp on CPU; transcription is a leaf utility so divergence cost is low. **Revisit when the Mac mini comes online** (see `reference_home_network.md`) — upstream's `whisper-cli` becomes the natural path then; swap is a one-file change in `transcription.ts`.
2. **Helper module placement: v2 core (`src/transcription.ts`).** The channel-agnostic `transcribeAudioBuffer(buffer)` is called by both WhatsApp and Telegram adapters, so it has to live somewhere both can import — only v2 core qualifies.
3. **OpenAI API key wiring: RESOLVED 2026-05-19.** v2's `src/env.ts` exposes `readEnvFile(keys: string[])` — API-identical to v1's helper (same `.env` lookup in `process.cwd()`, same opt-in pattern, deliberately does NOT load to `process.env` to prevent child-process leakage). v1's call `readEnvFile(['OPENAI_API_KEY'])` transfers verbatim. Transcription stays host-side (audio buffer lives in the channel adapter before container dispatch) so OneCLI's container-side secret-injection path is irrelevant here.

## Notes for Porter
- **Confirm before cherry-picking:** grep `upstream/main` for any newer transcription module. If `upstream/main` now ships a transcription module that supersedes `c054cbc`, drop the cherry-pick and use main's version instead. Commands: `git log upstream/main..whatsapp/skill/voice-transcription --oneline`; `git grep -l "transcribeAudio" upstream/main`.
- **Cherry-pick mechanics on whatsapp-fork/main:**
  - `git cherry-pick c054cbc` — expected single conflict on `package-lock.json` (verified via `git merge-tree`). Resolve with `git checkout --theirs package-lock.json && npm install && git add package-lock.json && git cherry-pick --continue`.
  - Do NOT cherry-pick `aa7f149` (openai v6 bump) unless needed — `whatsapp-fork/main` currently has no openai dep, so `c054cbc` will add it at whatever version was pinned in March 2026; verify and bump only if vulnerabilities or zod-v4 incompatibility surface.
  - Do NOT cherry-pick `0ccecbd` (local-whisper skill) — its `whisper-cli` invocation is macOS-Homebrew specific. Replay the Linux faster-whisper subprocess pattern from v1's `docs/v1-fork-reference/src/transcription.ts` lines ~30-90 by hand.
- **Fallback string** `[Voice Message - transcription unavailable]` is user-visible — preserve verbatim across all error paths.
- **`WHISPER_MODEL` env propagation** through the subprocess: v1 uses `{ ...process.env, WHISPER_MODEL: model }`. The subprocess only reads `WHISPER_MODEL` (and PATH for the Python binary); the OpenAI key is read separately via `readEnvFile`, so it does not leak into the faster-whisper subprocess.
- **Install-wiring deferred:** porting to `whatsapp-fork/main` + `telegram-fork/main` is the **intent target**. Making `/add-whatsapp` and `/add-telegram` actually pull from these forks is Phase 4/5 work — today `/add-whatsapp` runs `git fetch origin channels` (no `channels` branch exists in `noamrazbuilds/nanoclaw`; only `upstream/channels`). U1 leaves the install path untouched; the cutover plan handles wiring.
