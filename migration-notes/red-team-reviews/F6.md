# F6 — Red-team review (2026-06-04)

Adversarial review of the F6 reactions spec + port, objective "find what this spec missed." Reviewer: general-purpose agent with git access to the worktree and `a23e372`. Author addressed each finding before lock.

## Findings + resolutions

1. **[SHOULD-FIX] Wrong insertion total.** Spec said "342 insertions, 0 deletions (all edits)"; real diff is **462 insertions** across 8 files (342 in 6 modified + 120 in 2 net-new). → **Fixed** in `v2 hook point(s)`: now reads "462 insertions, 0 deletions across 8 files (342 in the 6 modified files + 120 in the 2 net-new files)." Verified `git -C …porter-F6 diff --shortstat whatsapp-fork/main..HEAD` → `8 files changed, 462 insertions(+)`.

2. **[SHOULD-FIX] Open-question #1 understated the failure mode.** The ported `whatsapp.ts` imports `getLatestMessage, storeReaction` from `../db.js`; v2 has no `src/db.ts`, so copying it into v2 as-is would *break import resolution*, not merely "lack a backend." → **Fixed**: open-question #1 rewritten to state the breakage explicitly, that v2-core threading is a **prerequisite** (not optional follow-up), with a "Timing safety" note (porter-F6 is local-only; fork `main` lacks the port; `add-whatsapp` fetches `whatsapp-fork/main`; so no current install is affected) and an explicit "do not push porter-F6 to fork main until the v2 thread lands."

3. **[NIT] Four read-path functions are dead code** (`getReactionsForMessage`, `getMessagesByReaction`, `getReactionsByUser`, `getReactionStats` — 0 call sites; consumed only by v1 tests, which decision #4 doesn't port). → **Noted** in the `src/db.ts` hook-point bullet: ported for byte-parity with `a23e372` and for `/add-reactions`-style skills; only `storeReaction`/`getLatestMessage`/`getMessageFromMe` are wired by this row.

4. **[NIT] Verify proves presence + typecheck, not behavior.** `verify-spec-F6.sh` does snippet grep + lockfile-diff + `tsc --noEmit`; it doesn't exercise a real reaction round-trip (DELETE-on-empty branch, auth `targetGroup.folder === sourceGroup`, latest-message fallback). The "manual smoke" half of the gate has no captured artifact. → **Accepted as-scoped** per decision #4 and U1/U2 precedent (gate = tsc + snippet + manual smoke). The typecheck does catch the import/wiring class of error (which is the meaningful guarantee within the fork). Behavioral round-trip is deferred to Checkpoint A manual smoke; the spec does not claim behavioral verification.

5. **[NIT] lock-spec Issue-1 grep brittleness** — keys on exact `git merge .*skill/` phrasing; not an F6 defect. → **No action** (F6's descriptive "merge `whatsapp/skill/reactions`" prose correctly does not trip the guard; flagged for future spec authors).

6. **[BLOCKER-for-process] Red-team artifact missing.** `migration-notes/red-team-reviews/F6.md` didn't exist; `lock-spec.sh` only `[WARN]`s on it. → **Fixed** — this file is that artifact, filed before lock per phase-2-revised-plan Q6.2 / Phase-2 step 2.

## Confirmed-OK (validated under git scrutiny)

- group-queue.ts is NOT reactions-related (`git show a23e372:src/group-queue.ts | grep -i reaction` empty) — spec correct.
- whatsapp-fork/main lacks all reactions infra (`git grep` empty) — spec correct.
- ipc-mcp-stdio.ts exists on the fork (modified, not created) — old-banner correction accurate.
- StatusTracker `sendReaction` (a23e372:index.ts:611, U3-DROPPED) correctly excluded; only the IpcDeps `sendReaction` (:672) ported.
- All ported code byte-faithful to `a23e372` modulo whitespace/idiom reflow — no dropped branches, no wrong variables, auth branch `isMain || targetGroup.folder === sourceGroup` exact.
- Port dependencies present in fork base (`is_from_me` column, `translateJid`, `registeredGroups()`, MCP scope vars).
- merge-tree "7 conflicting files" is exact (audit lists exactly 7 CONFLICT lines, full untruncated capture).
- Patch byte-matches worktree diff; both lockfiles byte-identical to base.

## Verdict

After fixes #1, #2, #3 and filing this artifact (#6): **lockable.** The port itself was faithful from the start; all defects were spec prose / process completeness, none in the ported code.
